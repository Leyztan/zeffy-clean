services:
  - type: web
    name: zeffy-clean
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
    startCommand: python app.py
    autoDeploy: true
